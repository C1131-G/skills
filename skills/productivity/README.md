# Productivity

Non-code workflow skills. Install via `npx skills@latest add C1131-G/skills`.

**Main skill** (invoke with modes: bare = audit report, `:check` = fix, `:write` = apply).

- **[read-research-paper](./read-research-paper/SKILL.md)** — three-pass paper reading (also via `skill-master`)
